# 更新日志

### 1.2.0

- feat: 新增独立倍速UP主设置, 设置后对应UP主将不再全局同步倍速. 可以仅保留通过快捷键设置倍速的功能. 适用于听歌等场景.
  - 添加 PlaybackRateSingle 类实现单页面独立倍速控制
  - 添加 renderSingleUpButton 渲染独立UP主倍速设置按钮
  - 添加 singleUpListStore 存储独立倍速UP主列表
  - 优化快捷键监听，使用 onKeydownMultiple 替代 onKeydown
  - 优化倍速切换逻辑，跳过 1.0 倍速中间值
  - 重构存储层，使用 createUserConfigStorage 和 GmArrayStorage
- feat: 修改用户配置的快捷键选择, 现在通过一个多选框选择. 优化脚本体积, 需要用户第一次加载完脚本之后才能够选择.
- refactor: 重构 PlaybackRate 模块，消除代码重复
  - 提取 PlaybackRateBaseClass 抽象基类，封装公共逻辑
  - 将三个策略类拆分到独立文件：PlaybackRateSync、PlaybackRateLocal、PlaybackRateSingle
  - 原 PlaybackRate 类重命名为 PlaybackRateLocal，语义更清晰
  - 添加 destroy() 方法修复内存泄漏问题
- fix: 修复热键迁移逻辑错误，消除重复赋值
- fix: 增强 main.ts 健壮性，添加元素存在性检查
- perf: 优化 initKeyboardListStore，避免每次脚本加载都强制写入存储
- chore: 创建 utils/ 目录，提取 getUpUidFromUrl 工具函数
- chore: 添加统一导出文件 (index.ts)，提高代码可维护性  
