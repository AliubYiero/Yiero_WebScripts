# 更新日志

### [prev] - update

- 当前视频没有字幕时, 请求 AI 摘要, 查看 AI 摘要是否存在字幕.

> https://api.bilibili.com/x/web-interface/view/conclusion/get?bvid=BV1JpAtekEBQ&cid=28499116662&up_mid=17967163&web_location=333.788&w_rid=927aa21bf89f685d4f517d9af676db11&wts=1740158572

### 1.5.2 - pref

- 修复如果已经存在时间轴的情况下, 手动导入字幕, 会生成两个时间轴容器的问题.

### 1.5.1 - pref

- 修复**开启结束时间显示**和**禁止选中**后, 仍然可以选中结束时间的问题.

### 1.5.0 - feat

- 支持手动导入字幕(.str / .ass), 字幕文件不会持久化保存.

### 1.4.2 - update

- 在时间轴的右上角, 添加一个隐藏的 X 按钮, 鼠标浮动到上面会显示, 点击可以关闭时间轴.

### 1.4.1 - fix

- 修复非分P视频, 无法加载时间轴的问题.

### 1.4.0 - fix

- 修复了脚本会导致大会员画质无法使用的问题.

> 由于 Bilibili 的风控限制, 2025/2/7 之后, 劫持 Bilibili 的播放器数据会导致大会员画质无法使用 ( v1.3.18 版本及以下会受到影响), 所以请及时更新至 v1.4.0 版本以上.

### 1.3.18 - fix

- 修复当鼠标移动到推荐视频时, 会导致时间轴改变为推荐视频的字幕的问题.

### 1.3.17 - pref

- 优化时间轴性能.

### 1.3.16 - fix

- 修复 [点击跳转(时间)], 在显示结束时间时, 只能通过点击开始时间跳转的问题.
- 修复 [复制时间] 无法复制结束时间的问题.
- (v1.3.15 无错误修复)

### 1.3.15 - fix

- 修复 [复制时间] 无法复制结束时间的问题.

### 1.3.14 - update

- 添加时间轴的结束时间, 默认隐藏.
- 并添加以下用户配置项:
    - 配置项:
        - **显示结束时间**: 是否显示时间轴的结束时间, 默认隐藏.
    - 网页样式:
        - **在时间前面显示图标**: 在时间前添加一个图标, 开始时间前是 "S" 图标, 结束时间前是 "E" 图标, 默认隐藏.

### 1.3.13 - pref

- 样式改变: 删除高亮时间轴时的上下边距增加.
- 在 `.timeline-header` 容器上添加上 `dataset` 属性.

### 1.3.12 - update

- 拓展了用户配置: **点击时间轴跳转视频** 的选项. 从开关勾选项变成下拉菜单选择项.

### 1.3.11 - pref

- 添加了更多可配置的用户配置:
    - 配置项:
        - **点击时间轴跳转视频**: 默认点击视频时间轴跳转将不再是一个默认行为, 现在可以控制了. 但是默认值仍然是开启状态.
    - 网页样式: 以下选项可以修改部分时间轴容器的样式
        - **显示字幕标题**: 是否显示字幕标题, 即 "时间轴 - 中文（自动生成）" 这一部分.
        - **显示子标题**: 是否显示字幕标题下方, 右侧的视频 id.
        - **显示容器按钮**: 是否显示字幕标题下方, 左侧的两个容器按钮.
        - **时间字体大小**: 修改时间文本的字体大小, 默认为 12px .
        - **文本内容字体大小**: 修改时间文本的字体大小, 默认为 14px .
        - **高亮文本内容字体大小**: 修改高亮状态的时间文本的字体大小, 默认为 16px .
        - **常规模式下的时间轴容器宽度**: 修改**常规模式**下, 时间轴容器的宽度, 默认为 411px , 该宽度和一般情况下的B站右侧的列表是等宽的.
        - **常规模式下的时间轴容器高度**: 修改**常规模式**下, 时间轴容器的**高度百分比**, 默认为 70vh, 即页面高度的 70% .
        - **网页全屏模式下的时间轴容器宽度**: 修改**网页全屏模式**下, 时间轴容器的宽度, 默认为 411px .

### 1.3.10 - update

- 更改容器选项文本: 时间轴居中 -> 时间轴锁定.
- 添加用户配置: 时间轴锁定位置(百分比), 默认为 30%.

### 1.3.9 - update

- 添加用户配置: **网页全屏显示**. 勾选后, 网页全屏将在右侧显示时间轴.

### 1.3.8 - update

- 现在切换 [时间轴居中] 选项时, 会立即锁定时间轴, 而不是等下一次切换时间段再锁定时间轴了.

### 1.3.7 - pref

- 修复当时间轴滚动到最后一个时间段时, 无法再次正确高亮时间轴的问题.

### 1.3.6 - update

- 添加用户配置: 禁止选中. 勾选该配置后, 当勾选快速复制(时间/文本)配置时, 对应内容将变成不可拖动选中状态.
- 优化时间段样式显示. 修复当文本超出一行时, 会挤压时间显示位置的问题.

### 1.3.5 - pref

- 修改两个时间段之间的行间距的表现方式. 现在的行间距会出现一段无法选中的空白, 导致用户需要比较精准的点击, 才能跳转对应的时间, 新的表现方式将优化这一问题.

### 1.3.4 - pref

- 更改时间轴居中的位置. 从 50% 改为 30% .
- 延长了时间轴容器加载到页面的时间, 避免其它内容加载错误.

### 1.3.3 - fix

- 更改时间轴容器添加到页面的方式, 避免其它内容加载错误.

### 1.3.2 - update

- 鼠标浮动到 [跳转空白] 选项时, 会显示当前视频空白时间总数(s).

### 1.3.1 - fix

- 修复开启选项 [跳转空白] 时, 无法通过点击时间轴, 跳转视频的问题.

### 1.3.0 - feat

- 添加按钮: 跳过空白. 开启后可跳过字幕之间的空白时间段.

> [注意]
>
> AI字幕可能因为识别问题, 导致跳过的空白时间段是可能存在内容的, 谨慎使用.
>
> 如果网速不行, 可能会因为频繁跳转视频, 视频加载导致卡顿, 反而比正常看花的时间更长.

### 1.2.3 - fix

- 优化了代码结构
    - 加快了时间轴容器的加载速度.
    - 修复时间轴容器可能重复出现的bug.
    - 修复在页面内切换视频时, 时间轴仍然是上一个视频的内容的问题.

### 1.2.2- pref

- 优化点击跳转时间轴的时候, 高亮时间轴会索引到指定时间轴的上一条时间轴, 导致高亮时间轴会"跳一下"的问题.
- 优化时间轴容器的加载速度.
- 优化页面性能.
    - 减少了极大量事件监听器的数量.
    - 现在"时间轴居中"按钮打开时, 将不再频繁滚动. 频繁滚动导致时间轴容器一直被锁定的问题已经被解决, 现在只有切换高亮时间轴的时候, 时间轴容器才会被锁定.

### 1.2.1- fix

- 修复在**初次载入页面**, **自动载入时间轴**时, 会生成两个时间轴容器的问题
- 修复在**页面内切换视频**时, 时间轴仍然是上一个视频的内容的问题.

### 1.2.0 - feat

- 添加脚本猫配置项:
    - 自动加载时间轴: 页面载入时, 自动加载时间轴到页面中
    - 自动复制时间: 点击时间的时候, 自动复制时间到粘贴板
    - 自动复制文本: 点击文本的时候, 自动复制文本到粘贴板

### 1.1.1 - update

- 添加开关: 时间轴居中. 默认开启. 关闭后会取消高亮时间轴居中.

### 1.1.0 - feat

- 现在时间轴对应的视频时间段播放的时候, 会高亮对应的时间轴.
- 高亮时间轴会始终保持在时间轴容器的中间.

### 1.0.1 - update

- 不再依赖脚本 [Bilibili Evolved]
- 现在 **更换分P / 合集 / 视频** 的时候, 会删除原来的时间轴UI, 并且菜单按钮会自动更新到新视频的时间轴了.

### 1.0.0 - feat

- 完成初步可实现的时间轴
- 依赖于脚本 [Bilibili Evolved]
